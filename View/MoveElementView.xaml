<UserControl x:Class="TChess2.View.MoveElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TChess2.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <WrapPanel>
        <TextBlock x:Name="TextBlockMoveCounter" Padding="{StaticResource dimPadding}" Margin="5,2,10,2" MinWidth="20"/>

        <TextBlock x:Name="TextBlockWhiteMove" Padding="{StaticResource dimPadding}" Margin="0,2,0,2" Cursor="Hand"
                   MinWidth="80"/>

        <TextBlock x:Name="TextBlockBlackMove" Padding="{StaticResource dimPadding}" Margin="20,2,10,2" Cursor="Hand"
                   MinWidth="80"/>
    </WrapPanel>
</UserControl>
