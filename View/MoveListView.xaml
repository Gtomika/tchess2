<UserControl x:Class="TChess2.View.MoveListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TChess2.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox>
        <GroupBox.Header>
            <TextBlock Text="{StaticResource strMoves}" FontSize="{StaticResource dimFontHeader}"/>
        </GroupBox.Header>
        <ScrollViewer>
            <!-- Moves are added dynamically. -->
            <StackPanel x:Name="StackPanelMoves">
                <!-- Each row is a MoveElementView -->
            </StackPanel>
        </ScrollViewer>
    </GroupBox>
</UserControl>
